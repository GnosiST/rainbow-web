# Optional: enables generated AI Guide responses in the Projects window.
OPENAI_API_KEY=

# Optional: defaults to gpt-4.1-mini when omitted.
OPENAI_MODEL=gpt-4.1-mini

# Optional: defaults to gpt-image-2 when omitted.
OPENAI_IMAGE_MODEL=gpt-image-2
